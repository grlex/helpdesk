

{% include 'request/status_actions.twig' %}
{{ restore_context() }}

{% set tablePanelConfig = {
        'fields': ['id','name','textStatus'],

        'actions':{
            view: {
                baseUri: '/request/view',
                title: 'items._common.view'|trans,
                text: 'items._common.view'|trans,
                class: ' fa fa-eye',
                position: 'body',
            }
        }|merge(status_actions)
} %}

{%  include 'commonEntity/list.html.twig' %}


